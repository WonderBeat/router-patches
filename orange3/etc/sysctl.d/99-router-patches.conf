# TCP timestamps off, matching the asus and the xiaomi: the timestamp option is
# a DPI fingerprint and leaks uptime. systemd-sysctl.service reads this at boot,
# so unlike the routers there is no cron needed to keep it applied.
net.ipv4.tcp_timestamps = 0
